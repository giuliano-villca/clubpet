<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".detalle_animal">

    <!--  nav superior -->
    <LinearLayout
        android:id="@+id/navSuperior"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="8dp"
        android:orientation="horizontal"
        android:elevation="6dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

    <!-- Barra de búsqueda -->
    <SearchView
        android:id="@+id/searchView"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_weight="1"
        android:queryHint="Buscar..."
        android:iconifiedByDefault="false" />

    <!-- Spinner -->
    <Spinner
        android:id="@+id/spinnerProvincia"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp" />

    <!-- Botón Notificaciones -->
    <ImageButton
        android:id="@+id/btnNotificaciones"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:background="@android:color/transparent"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/noti"/>

    <!-- Botón Perfil -->
    <ImageButton
        android:id="@+id/btnPerfil"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="4dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/logoperfil"/>

    </LinearLayout>

    <!-- contenido principal con scroll -->
    <ScrollView
        android:id="@+id/scrollContenido"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/navSuperior"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:padding="16dp">

            <!--foto grande -->
            <FrameLayout
                android:layout_width="300dp"
                android:layout_height="300dp"
                android:background="#F0F0F0"
                android:elevation="4dp">

                <ImageView
                    android:id="@+id/imgPrincipal"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:src="@drawable/perromaau"/>

            </FrameLayout>

            <!--mini foto -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="12dp">

                <ImageView
                    android:id="@+id/imgMini1"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:layout_margin="4dp"
                    android:background="#EEEEEE"
                    android:scaleType="centerCrop"
                    android:src="@drawable/perromaau" />

                <ImageView
                    android:id="@+id/imgMini2"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:layout_margin="4dp"
                    android:background="#EEEEEE"
                    android:scaleType="centerCrop"
                    android:src="@drawable/perromaau" />

                <ImageView
                    android:id="@+id/imgMini3"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:layout_margin="4dp"
                    android:background="#EEEEEE"
                    android:scaleType="centerCrop"
                    android:src="@drawable/perromaau" />
            </LinearLayout>

            <!-- nombre del animal -->
            <TextView
                android:id="@+id/txtNombreAnimal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="NOMBRE DEL ANIMAL"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#000000" />

            <!-- boton para adoptar -->
            <Button
                android:id="@+id/btnAdoptar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="ADOPTAR"
                android:layout_marginTop="8dp"
                android:backgroundTint="#CCCCCC"
                android:textColor="#000000" />

            <!-- descripcion -->
            <TextView
                android:id="@+id/txtDescripcionTitulo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="Descripción:"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/txtDescripcion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Información información información información información información información información información información información información información información información."
                android:textColor="#333333"
                android:textSize="15sp" />

        </LinearLayout>
    </ScrollView>

    <!-- nav inferior -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        style="@style/BottomNavCentered"
        android:background="@color/blue"
        app:itemTextColor="@color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:menu="@menu/bottom_nav_menu" />
</androidx.constraintlayout.widget.ConstraintLayout>